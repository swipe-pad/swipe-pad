Run the complete verification suite:

1. `bun run typecheck` - TypeScript check
2. `bun run lint` - ESLint
3. `bun run lint:css` - Stylelint
4. `bun run knip` - Dead code detection
5. `bun run test` - Unit tests

Report results and any failures.
