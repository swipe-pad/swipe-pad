# ClickUp Integration
CLICKUP_API_TOKEN=your_clickup_api_token
CLICKUP_CLIENT_SECRET=your_clickup_client_secret

# Thirdweb Configuration
# Client ID is public - used in browser for wallet connection
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_thirdweb_client_id
NEXT_PUBLIC_APP_URL=https://app.swipepad.xyz
NEXT_PUBLIC_FARCASTER_MINIAPP_ENABLED=1

# Secret Key is server-only - used for account abstraction backend
THIRDWEB_SECRET_KEY=your_thirdweb_secret_key
FARCASTER_ACCOUNT_ASSOCIATION_JSON={"header":"...","payload":"...","signature":"..."}
NEYNAR_API_KEY=your_neynar_api_key

# Convex Configuration
# Automatically set by `bun convex dev`, but needed for production/CI
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud
# Optional (dev only): if set to 1, project catalog reads are cache-only (no Convex fetch)
NEXT_PUBLIC_CONVEX_CACHE_ONLY_DEV=0
# Optional: set to 0 to disable temporary feed fallback to getAllProjects
CONVEX_FEED_ALLOW_LEGACY_FALLBACK=1

# Admin Controls (Convex)
ADMIN_API_KEY=your_admin_api_key
ADMIN_WALLETS=0xabc...,0xdef...
# Optional (tests): wallet to use in seed script when ADMIN_WALLETS enforcement is enabled
ADMIN_E2E_CALLER_WALLET=0xabc...

# Optional (tests): set to 1 to require feed data in e2e routing test
E2E_REQUIRE_FEED_DATA=0

# Talent Protocol Integration
# Required for syncing builder scores (v3 API)
TALENT_PROTOCOL_API_KEY=your_talent_protocol_api_key

# Foundry / Deploy
CELO_RPC=https://forno.celo.org
BASE_RPC=https://mainnet.base.org
CELOSCAN_KEY=your_celoscan_api_key
BASESCAN_KEY=your_basescan_api_key
PRIVATE_KEY=your_deployer_private_key
OWNER_ADDRESS=0x...
TOKEN_ADDRESS=0x...
CELO_MULTISIG=0x...
BASE_MULTISIG=0x...
